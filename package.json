{
  "name": "shannon-softball-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Static collegiate softball recruiting portfolio for Shannon Hall.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/style.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./dist/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
